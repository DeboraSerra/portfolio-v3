import { render } from "@testing-library/react";
import {{pascalCase component}} from "./{{pascalCase component}}";
import TProvider from "../helpers/ThemeProvider";

describe("Test the {{pascalCase component}} component", () => {
  it('Should match the snapshot', () => {
    const { asFragment } = render(<TProvider><{{pascalCase component}} /></TProvider>)
    expect(asFragment()).toMatchSnapshot();
  })
})